version: '1.0'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always

  worker:
    build: .
    command: python worker.py
    env_file:
      - .env
    restart: always